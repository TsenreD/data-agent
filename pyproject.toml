[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "data-agent"
version = "0.1.0"
description = "Sequential data collection agent scaffold with optional Ollama-backed extraction."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "beautifulsoup4>=4.12",
  "datasets>=2.20",
  "ddgs>=9.0",
  "docker>=7.1",
  "httpx>=0.28",
  "matplotlib>=3.8",
  "openai>=2.28",
  "pandas>=2.2",
  "pyyaml>=6.0",
  "requests>=2.32",
  "scrapy>=2.11",
  "seaborn>=0.13",
  "smolagents>=1.24.0",
  "websocket-client>=1.9",
]

[project.scripts]
data-agent = "cli:main"
data-agent-collect = "cli:main"

[project.optional-dependencies]
dev = [
  "black>=24.0",
  "ipython>=8.0",
  "mypy>=1.8",
  "pytest>=8.0",
  "pytest-asyncio>=0.23",
  "ruff>=0.5",
]

[tool.setuptools]
py-modules = ["cli"]

[tool.setuptools.packages.find]
include = ["agents*", "executor*", "models*"]

[tool.setuptools.package-data]
"agents.data_collection" = ["Dockerfile", "EdaDockerfile", "skills/*/SKILL.md"]
